(function(){'use strict';const KEY='mk_wiki_guides_v1';const validId=id=>typeof id==='string'&&/^[a-z][a-z0-9-]{1,99}$/.test(id);function readPreferences(storage){try{const value=JSON.parse(storage.getItem(KEY)||'{}');return new Set(Array.isArray(value.hidden)?value.hidden.filter(validId).slice(0,500):[]);}catch(_){return new Set();}}
function writePreference(storage,previous,id,hidden){const next=new Set(previous);if(!validId(id))return{hidden:next,saved:false};if(hidden)next.add(id);else next.delete(id);try{const value=JSON.stringify({version:1,hidden:[...next].sort()});storage.setItem(KEY,value);return{hidden:next,saved:storage.getItem(KEY)===value};}catch(_){return{hidden:next,saved:false};}}
function fitCard(view,size,target){const pad=12,w=Math.min(size.width,view.width-pad*2),h=Math.min(size.height,view.height-pad*2);const clamp=(v,lo,hi)=>Math.max(lo,Math.min(hi,v));let x=view.width-w-pad,y=view.height-h-pad;if(target&&view.width>=720){x=target.right+w+pad*2<=view.width?target.right+pad:target.left-w-pad;y=target.top;if(x<pad){x=view.width-w-pad;y=target.bottom+pad;}}
if(target&&view.width<720&&target.top>view.height*.53)y=pad;return{x:clamp(x,pad,view.width-w-pad),y:clamp(y,pad,view.height-h-pad)};}
if(typeof module!=='undefined'&&module.exports)module.exports={KEY,validId,readPreferences,writePreference,fitCard};if(typeof document==='undefined'||window.MkGuides)return;const drawings={search:'<rect x="28" y="18" width="264" height="38" rx="12"/><circle cx="51" cy="35" r="8"/><path d="m57 41 7 7M80 36h120M45 80h92m-92 24h184m-184 24h146"/>',map:'<path d="M65 92 156 37 258 93M65 92 158 132 258 93M156 37v95"/><rect x="18" y="76" width="94" height="32" rx="16"/><rect x="109" y="21" width="94" height="32" rx="16"/><rect x="211" y="77" width="94" height="32" rx="16"/><circle cx="158" cy="132" r="16"/>',list:'<rect x="33" y="18" width="254" height="128" rx="15"/><path d="m52 48 6 6 12-14m-18 42 6 6 12-14m-18 42 6 6 12-14M89 47h158M89 81h128M89 115h148"/>',settings:'<path d="M35 40h250M35 83h250M35 126h250"/><circle cx="109" cy="40" r="13"/><circle cx="220" cy="83" r="13"/><circle cx="81" cy="126" r="13"/>',privacy:'<path d="M160 15 222 38v41q0 47-62 70-62-23-62-70V38Z"/><rect x="137" y="71" width="46" height="38" rx="8"/><path d="M146 71V58a14 14 0 0 1 28 0v13m-14 17v8"/>',shop:'<rect x="34" y="38" width="74" height="92" rx="12"/><rect x="123" y="20" width="74" height="110" rx="12"/><rect x="212" y="38" width="74" height="92" rx="12"/><path d="m61 67 10-8 10 8v28H61Zm89-19 10-8 10 8v30h-20Zm89 19 10-8 10 8v28h-20ZM54 112h34m55 0h34m55 0h34"/>',progress:'<path d="M35 132h254M57 131V96h36v35m32 0V65h36v66m32 0V29h36v102M43 74l68-26 59-17 74-14"/>',share:'<circle cx="75" cy="82" r="28"/><circle cx="241" cy="40" r="25"/><circle cx="241" cy="123" r="25"/><path d="m103 75 112-28M103 90l112 28m-44-78 14 10-8 15m-6 39 14 10-14 10"/>',profile:'<rect x="41" y="18" width="238" height="128" rx="18"/><circle cx="103" cy="63" r="20"/><path d="M70 117v-8a33 26 0 0 1 66 0v8m24-63h86m-86 26h65m-65 26h78"/>',practice:'<rect x="66" y="17" width="188" height="132" rx="14"/><path d="M91 45h137M91 68h114m-92 29 7 7 12-15M129 99h97m-128 27h128"/>'};const settings=[{id:'startup-items',title:'Choose your tools',kind:'panel',priority:300,surface:'#mk-startup-menu:not([hidden])',when:()=>!window.__mkExamMode,steps:[{title:'A lighter workspace',text:'Choose which tools load on this browser. Reading and basic search remain available.',target:'.mk-startup-list',visual:'settings',action:'Review the switches before changing them.'},{title:'Your account tools',text:'Account / sync enables your workspace, comments, rankings and shop. Saving study sets and review queues also needs this tool.',target:'[data-feature="account"]',visual:'profile'},{title:'Follow the connections',text:'Concept connections adds the side panel, maps and guided routes.',target:'[data-feature="learningPath"]',visual:'map'},{title:'Record your understanding',text:'Mastery / rating adds personal ratings, readiness and revision tools. AI quiz needs Mastery to be enabled.',target:'[data-feature="mastery"]',visual:'progress'},{title:'Check an answer',text:'AI quiz adds practice questions where a check is available. Answer checking requires an internet connection.',target:'[data-feature="aiQuiz"]',visual:'practice'},{title:'Organise your course list',text:'Sidebar sorter adds course selection and sorting to the concept sidebar.',target:'[data-feature="sidebarSort"]',visual:'list'},{title:'Lite or Defaults',text:'Lite turns optional tools off. Defaults brings them back. Neither choice deletes your stored learning records.',target:'[data-action="lite"]',visual:'settings'},{title:'Apply your choice',text:'Check for Saved, then Reload to apply the full change. If saving fails, use Retry before reloading.',target:'[data-action="reload"]',visual:'settings',action:'Reload only when you are ready to leave this page.'}]},{id:'startup-items-exam',title:'Tools in Exam Mode',kind:'panel',priority:300,surface:'#mk-startup-menu:not([hidden])',when:()=>!!window.__mkExamMode,steps:[{title:'A fixed set of tools',text:'This mirror fixes the available tools for Exam Mode. Changing these switches cannot enable accounts, cloud sync or AI checks here.',target:'.mk-startup-list',visual:'settings'},{title:'Study tools stay local',text:'Concept connections, maps, local mastery and the course sidebar are available. These local records do not sync to an account on this mirror.',target:'[data-feature="learningPath"]',visual:'map'},{title:'Keep your main-site choices',text:'Exam Mode does not overwrite the startup choices you made on the normal Wiki. Close this menu to continue using the available course tools.',target:'.mk-startup-list',visual:'privacy'}]}];let storage;try{storage=window.localStorage;}catch(_){storage=null;}
let hidden=readPreferences(storage),context=null,activation='',dismissed=false;let root,card,spotlight,launcher,live,tour=null,index=0,library=false,previousFocus=null,compact=false;let timer=0,frame=0,lastUserGesture=0,started=false;const selectAll=(scope,selector)=>{try{return[...scope.querySelectorAll(selector)];}catch(_){return[];}};function visible(el){if(!el||!el.isConnected||el.closest('[hidden],[aria-hidden="true"],details:not([open]) > :not(summary)'))return false;const css=getComputedStyle(el);return css.display!=='none'&&css.visibility!=='hidden'&&css.opacity!=='0'&&!!el.getClientRects().length;}
function catalog(){const seen=new Set();return[...(window.MkGuidePages||[]),...(window.MkGuideAccount||[]),...settings].filter(entry=>{if(!validId(entry.id)||seen.has(entry.id)||!Array.isArray(entry.steps)||!entry.steps.length)return false;seen.add(entry.id);return true;});}
function surfaceFor(entry){if(entry.path){try{if(!(new RegExp(entry.path)).test(location.pathname))return null;}catch(_){return null;}}
return selectAll(document,entry.surface||'main').find(el=>!el.closest('[data-wiki-guide-ui]')&&visible(el)&&(!entry.when||entry.when(el)))||null;}
function chooseContext(){const focused=document.activeElement;return catalog().map(entry=>({entry,surface:surfaceFor(entry)})).filter(item=>item.surface).sort((a,b)=>(Number(b.entry.priority)||0)-(Number(a.entry.priority)||0)||Number(b.surface.contains(focused))-Number(a.surface.contains(focused))||Number(b.entry.id===context?.entry.id)-Number(a.entry.id===context?.entry.id))[0]||null;}
function element(tag,cls,text){const node=document.createElement(tag);if(cls)node.className=cls;if(text!=null)node.textContent=text;return node;}
function button(text,action,cls=''){const node=element('button','wg-button '+cls,text);node.type='button';node.dataset.wgAction=action;return node;}
function mount(){if(root){if(!root.isConnected)document.body.append(root);return;}
root=element('div','wg-root');root.id='wiki-guide-root';root.dataset.wikiGuideUi='true';root.setAttribute('popover','manual');spotlight=element('div','wg-spotlight');spotlight.hidden=true;spotlight.setAttribute('aria-hidden','true');card=element('section','wg-card');card.hidden=true;card.setAttribute('role','dialog');card.setAttribute('aria-modal','false');card.setAttribute('aria-labelledby','wg-title');launcher=button('Guide','launch','wg-launcher');launcher.setAttribute('aria-label','Guide for this screen');launcher.setAttribute('aria-haspopup','dialog');live=element('span','wg-sr');live.setAttribute('role','status');live.setAttribute('aria-live','polite');root.append(spotlight,card,launcher,live);document.body.append(root);root.addEventListener('click',handleClick);root.addEventListener('change',event=>{if(event.target.dataset.wgRemember!=null&&tour)remember(tour.entry.id,event.target.checked);});root.addEventListener('keydown',event=>{if(event.key==='Escape'&&!card.hidden){event.preventDefault();event.stopPropagation();close(true);}});}
function placeRoot(surface,kind=context?.entry.kind){const parent=surface&&kind==='panel'?surface:document.body;if(root.parentNode!==parent){try{if(root.matches(':popover-open'))root.hidePopover();}catch(_){}
parent.append(root);}
try{if(typeof root.showPopover==='function'&&!root.matches(':popover-open'))root.showPopover();}catch(_){}}
function remember(id,value){const result=writePreference(storage,hidden,id,value);hidden=result.hidden;const message=result.saved?(value?'This guide will stay off on this browser. You can replay it from Guide.':'This guide will appear when you open this screen again.'):'Browser storage is unavailable. This choice lasts for this tab only.';const note=card.querySelector('.wg-save-status');if(note)note.textContent=message;live.textContent=message;}
function resolveTarget(step){if(!tour||!step?.target)return null;const local=selectAll(tour.surface,step.target);try{if(tour.surface.matches(step.target))local.unshift(tour.surface);}catch(_){}
return[...local,...selectAll(document,step.target)].find(el=>!el.closest('[data-wiki-guide-ui]')&&visible(el))||null;}
function illustration(step){const box=element('div','wg-illustration');box.innerHTML='<svg viewBox="0 0 320 165" aria-hidden="true" focusable="false">'+(drawings[step.visual]||drawings.list)+'</svg>';box.append(element('span','wg-illustration-label',step.action||'Follow the highlighted part of this screen'));if(step.example)box.append(element('code','wg-example',step.example));return box;}
function renderStep(focus=false){if(!tour)return;const steps=tour.steps||tour.entry.steps;index=Math.max(0,Math.min(index,steps.length-1));const step=steps[index];card.replaceChildren();card.hidden=false;card.dataset.guideId=tour.entry.id;card.dataset.step=String(index+1);card.dataset.compact=String(compact);const top=element('div','wg-top');top.append(element('span','wg-kicker',tour.entry.title),button('Skip','skip','wg-skip'));const heading=element('h2','',step.title);heading.id='wg-title';const progress=element('div','wg-progress');progress.setAttribute('aria-label',`Step ${index + 1} of ${steps.length}`);steps.forEach((part,i)=>{const dot=button(String(i+1),'step','wg-step'+(i===index?' is-current':''));dot.dataset.step=String(i);dot.setAttribute('aria-label',`Step ${i + 1}: ${part.title}`);if(i===index)dot.setAttribute('aria-current','step');progress.append(dot);});const description=element('p','wg-description',step.text);description.id='wg-description';card.setAttribute('aria-describedby',description.id);const target=resolveTarget(step);card.append(top,progress,heading,illustration(step),description);if(step.target){const hint=element('p','wg-target-note',target?'Use Show me to uncover the highlighted control.':'This control appears in its relevant view. You can continue the guide.');card.append(hint);}
const foot=element('div','wg-controls');const back=button('Back','back');back.disabled=index===0;foot.append(back);if(target)foot.append(button(compact?'Full guide':'Show me','show'));foot.append(button(index===steps.length-1?'Done':'Next','next','wg-primary'));const label=element('label','wg-remember');const check=element('input');check.type='checkbox';check.dataset.wgRemember='';check.checked=hidden.has(tour.entry.id);label.append(check,document.createTextNode('Don’t show this guide again'));const status=element('p','wg-save-status');status.setAttribute('role','status');card.append(foot,label,status,button('All guides','library','wg-library-link'));launcher.hidden=true;position();live.textContent=`${tour.entry.title}. Step ${index + 1} of ${steps.length}: ${step.title}`;if(focus)card.querySelector('[data-wg-action="next"]')?.focus({preventScroll:true});}
function open(item,manual=false){if(!item)return;mount();tour={...item};library=false;index=0;compact=false;previousFocus=document.activeElement;tour.steps=item.entry.steps.filter(step=>!step.optional||resolveTarget(step));if(!tour.steps.length)tour.steps=[{title:'Nothing here yet',text:'This screen fills as you use the Wiki. Return after saving an item or recording some activity.',visual:'list'}];placeRoot(item.surface,item.entry.kind);renderStep(manual);}
function close(restore){if(!root)return;card.hidden=true;spotlight.hidden=true;launcher.hidden=false;tour=null;library=false;dismissed=true;if(restore&&previousFocus?.isConnected){try{previousFocus.focus({preventScroll:true});}catch(_){}}}
function openLibrary(){mount();library=true;tour=null;compact=false;card.dataset.compact='false';spotlight.hidden=true;launcher.hidden=true;card.hidden=false;card.replaceChildren();card.dataset.guideId='library';card.removeAttribute('aria-describedby');const head=element('div','wg-top');const title=element('h2','','Your guides');title.id='wg-title';head.append(title,button('Close','skip'));const summary=element('p','wg-description','Replay this screen’s guide, or restore a reminder you turned off. Other guides appear when you open their screen.');const list=element('div','wg-library');catalog().forEach(entry=>{const row=element('div','wg-library-row');const copy=element('div');copy.append(element('strong','',entry.title),element('small','',hidden.has(entry.id)?'Automatic reminder off':'Automatic reminder on'));row.append(copy);const available=surfaceFor(entry);if(available){const play=button('Replay','replay');play.dataset.guide=entry.id;row.append(play);}
if(hidden.has(entry.id)){const restore=button('Restore','restore');restore.dataset.guide=entry.id;row.append(restore);}
list.append(row);});card.append(head,summary,list);position();head.querySelector('button')?.focus({preventScroll:true});}
function handleClick(event){const control=event.target.closest('[data-wg-action]');if(!control)return;event.preventDefault();event.stopPropagation();const action=control.dataset.wgAction;if(action==='launch'){if(context)open(context,true);else openLibrary();}
if(action==='skip')close(true);if(action==='library')openLibrary();if(action==='restore'){remember(control.dataset.guide,false);openLibrary();}
if(action==='replay'){const entry=catalog().find(item=>item.id===control.dataset.guide);const surface=entry&&surfaceFor(entry);if(surface)open({entry,surface},true);}
if(!tour)return;if(action==='back'){index--;compact=false;renderStep(true);}
if(action==='step'){index=Number(control.dataset.step);compact=false;renderStep(true);}
if(action==='next'){if(index===tour.steps.length-1)close(true);else{index++;compact=false;renderStep(true);}}
if(action==='show'){compact=!compact;renderStep();if(!compact)return;const target=resolveTarget(tour.steps[index]);if(target){target.scrollIntoView({behavior:'auto',block:'center',inline:'nearest'});const focusable=target.matches('button,input,select,textarea,a[href],[tabindex],summary')?target:selectAll(target,'button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),a[href],[tabindex],summary').find(visible);try{focusable?.focus({preventScroll:true});}catch(_){}
schedulePosition();}}}
function position(){frame=0;if(!root||card.hidden)return;const vv=window.visualViewport,width=vv?.width||innerWidth,height=vv?.height||innerHeight;const ox=vv?.offsetLeft||0,oy=vv?.offsetTop||0;card.style.maxHeight=Math.max(160,width<720&&!library?height*.62:height-28)+'px';const target=tour&&resolveTarget(tour.steps[index]);let rect=target?.getBoundingClientRect();if(rect&&(rect.bottom<=oy||rect.top>=oy+height||rect.right<=ox||rect.left>=ox+width))rect=null;const box=rect?{left:Math.max(0,rect.left-ox),top:Math.max(0,rect.top-oy),right:Math.min(width,rect.right-ox),bottom:Math.min(height,rect.bottom-oy)}:null;const at=fitCard({width,height},{width:card.offsetWidth||350,height:card.offsetHeight||400},box);card.style.left=at.x+ox+'px';card.style.top=at.y+oy+'px';if(box&&box.right>box.left&&box.bottom>box.top){spotlight.hidden=false;spotlight.style.cssText=`left:${box.left + ox - 4}px;top:${box.top + oy - 4}px;width:${box.right - box.left + 8}px;height:${box.bottom - box.top + 8}px`;}else spotlight.hidden=true;}
function schedulePosition(){if(!frame&&root&&!card.hidden)frame=requestAnimationFrame(position);}
function scan(){timer=0;if(performance.now()-lastUserGesture<200){schedule();return;}
mount();const next=chooseContext(),key=next?location.pathname+':'+next.entry.id:location.pathname+':none';if(key!==activation){activation=key;context=next;dismissed=false;close(false);dismissed=false;placeRoot(tour?.surface||next?.surface,tour?.entry.kind||next?.entry.kind);if(next&&!hidden.has(next.entry.id))open(next);}else{context=next;if(tour&&next&&tour.entry.id===next.entry.id)tour.surface=next.surface;placeRoot(tour?.surface||next?.surface,tour?.entry.kind||next?.entry.kind);if(!dismissed&&!tour&&!library&&next&&!hidden.has(next.entry.id))open(next);}
launcher.setAttribute('aria-label',context?`Guide: ${context.entry.title}`:'Open guides');}
function schedule(){if(timer)clearTimeout(timer);timer=setTimeout(scan,240);}
function boot(){if(started){activation='';schedule();return;}
started=true;mount();for(const type of['pointerdown','mousedown','click'])window.addEventListener(type,event=>{if(!event.target?.closest?.('[data-wiki-guide-ui]'))return;event.stopImmediatePropagation();if(type==='click')handleClick(event);},true);window.addEventListener('keydown',event=>{if(event.key!=='Escape'||!root||card.hidden)return;event.preventDefault();event.stopImmediatePropagation();close(true);},true);const observer=new MutationObserver(records=>{if(records.some(record=>{const el=record.target.nodeType===1?record.target:record.target.parentElement;return el&&!el.closest('[data-wiki-guide-ui],.lp-mapstage,.lp-mapviewport')&&!(record.type==='childList'&&[...record.addedNodes,...record.removedNodes].every(n=>n.nodeType!==1||n.dataset?.wikiGuideUi));}))schedule();});observer.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden','open','data-type','data-view','data-lp-mode','aria-selected','aria-expanded','class']});document.addEventListener('pointerdown',event=>{if(!event.target.closest('[data-wiki-guide-ui]'))lastUserGesture=performance.now();},{passive:true,capture:true});document.addEventListener('focusin',event=>{if(!event.target.closest('[data-wiki-guide-ui]'))schedule();},true);document.addEventListener('click',event=>{if(!event.target.closest('[data-wiki-guide-ui]'))schedule();},true);window.addEventListener('resize',schedulePosition,{passive:true});document.addEventListener('scroll',schedulePosition,{passive:true,capture:true});window.visualViewport?.addEventListener('resize',schedulePosition,{passive:true});window.visualViewport?.addEventListener('scroll',schedulePosition,{passive:true});window.addEventListener('storage',event=>{if(event.key===KEY||event.key===null){hidden=readPreferences(storage);schedule();}});window.addEventListener('popstate',()=>{activation='';schedule();});document.addEventListener('DOMContentSwitch',()=>{activation='';close(false);schedule();});schedule();}
window.MkGuides={open:id=>{const entry=catalog().find(item=>item.id===id);const surface=entry&&surfaceFor(entry);if(surface)open({entry,surface},true);},library:openLibrary,refresh:schedule};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();if(typeof document$!=='undefined')document$.subscribe(()=>{if(started){activation='';close(false);schedule();}});})();